# Copie para .env.production antes de `npm run build`.
# A URL e a chave "anon" são públicas por desenho: vão no JavaScript do
# navegador. Quem protege os dados é a RLS do banco, não o segredo delas.
# A chave fica em: Supabase → Project Settings → API → "anon public".
VITE_SUPABASE_URL=https://jqgcgaalqabmnevadqfy.supabase.co
VITE_SUPABASE_ANON_KEY=cole-aqui-a-chave-anon
