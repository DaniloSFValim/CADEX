# Endereço e chave PÚBLICA (anon) do Supabase. A anon key é feita para o
# navegador; quem protege os dados são as regras de acesso (RLS) do banco.
# NUNCA coloque aqui a service_role key.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
